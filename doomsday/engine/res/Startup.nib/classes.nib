{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {closeNow = id; }; 
            CLASS = StartupWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {startupText = NSTextView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}