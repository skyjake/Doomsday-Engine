# jHexen: Map Info
#
# Note that here we only have a wildcard Map Info. The real data is
# read from Hexen's MAPINFO lump.

Header { Version = 6; }

# We need this only with the 4-level demo.
SkipIf Not hexen-demo;

Map Info {
  ID = "*";
  Gravity = 1;
}